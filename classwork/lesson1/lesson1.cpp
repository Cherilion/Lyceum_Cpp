#include <iostream>
#include <windows.h>
#include <string>
#include <cmath>
using namespace std;

int main()
{
    /*
    cout << "Input expression: ";
    string str;
    cin >> str;

    int a, b, c;

    string tmp;

    for(int i = 0; i < sizeof(str); i++ )
    {
        if (i == 0 || (str[i-1] != '^'))
            while(isdigit(str[i]))
            {
                tmp += str[i];
                i++;
                if(!(isdigit(str[i])))
                {
                    switch(str[i+2])
                    {
                    case 2:
                        a = int(tmp);
                    case 1:
                        b = int(tmp);
                    case 0:
                        c = int(tmp);
                    }
                    i = i+3;
                    beak;
                }
            }
        double D = pow(b,2) - (4*a*c);
        int x[2];
        if(D >= 0)
        x[0] = (-b + sqrt(D)) / (2*a);
        x[] = (-b - sqrt(D)) / (2*a);

        cout << "#"*20 << '/n';
        if(D >= 0)
            cout << "x-1 = " << x[0] << '/n'
                 << "x-2 = " << x[1] << endl;
        else
            cout << "no roots"
   */

   cout << "input number: ";
   int num;
   cin >> num;
   cout << "tenth: " << num / 10 << '\n' << "units: " << num % 10 << endl;
    system("pause");
    return 0;
}




/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
////////////////// ////////////////////////////////////////////// ///////////////////////////////////////////////////
//////////////// // //////////////////////////////////////////  // //////////////////////////////////////////////////
//////////////  ///  //////////////////////////////////////// //// //////////////////////////////////////////////////
////////////   ////  //////////////////////////////////////  ///// //////////////////////////////////////////////////
/////////     //////   //////////////////////////////////  ///////  /////////////////////////////////////////////////
///////    /////////    ///////////////////////////////  //////////  ////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
/////////////////////////////////////////////////////////////////////////////////////////////////////////////////////
////////////////////////////                      ///////////////////////////////////////////////////////////////////
////////////////////////////                      


































